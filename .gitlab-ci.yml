image: node:22

stages:
  - deploy

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
  pages:
    publish: dist
  artifacts:
    paths:
      - dist
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'